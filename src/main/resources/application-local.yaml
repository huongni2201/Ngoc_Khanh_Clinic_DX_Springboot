spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/nkclinic}
    username: ${DB_USERNAME:nkclinic}
    password: ${DB_PASSWORD:local_dev_only_change_me}
  flyway:
    enabled: true
    default-schema: public
    schemas: public
